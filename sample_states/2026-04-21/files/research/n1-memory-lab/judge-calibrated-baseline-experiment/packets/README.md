# Packets

Use this directory for bounded-packet configurations.

Each packet config should state:

- `packet_bound`;
- visible sources;
- tool policy;
- retrieval policy;
- memex visibility;
- synthesis visibility;
- git-anchor visibility;
- reference time behavior.

`zero` packet configs belong here as first-class bounded conditions.

