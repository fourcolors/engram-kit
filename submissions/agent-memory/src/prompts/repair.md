Your DRAFT answer hedged that one or more files are unavailable / not shown /
cannot be determined. That was wrong — those files ARE present in the workspace,
and their full contents are below.

Rewrite the answer to USE this content and DELETE every false "unavailable /
not shown / cannot determine" claim about these specific files. Ground the
dependent claims (definitions, versions, dates, counts, transitions) in the
actual text below.

Change ONLY what depended on these now-available files. Keep every other correct
claim exactly as written. Do NOT introduce any new hedges, and do not soften
claims that are already correct.

REFERENCE TIME: <<REFERENCE_TIME>>  (use only information knowable by this time)

QUESTION:
<<QUESTION>>

FILES THE DRAFT WRONGLY CALLED UNAVAILABLE (full contents — these ARE present):
<<FILES>>

DRAFT ANSWER (JSON):
<<DRAFT>>

Output ONLY the corrected JSON object — no prose, no code fences — with keys
answer, evidence_paths, memory_refs, uncertainty.
