# Presentation Placement

Share this kit on the existing **Participant packet** slide near the end of the
deck.

Deck path:

```text
research/n1-memory-lab/engram-event-work/presentation/index.html
```

Current slide:

```text
aria-label="Participant packet"
```

Use that slide after the workshop/discussion slide and before the close. The
talk has already explained the environment and scoring by then, so this is the
right moment to say:

```text
Take-home kit:
./run.sh update STATE_DIR MEMORY_DIR
./run.sh answer STATE_DIR MEMORY_DIR QUESTION_JSON ANSWER_JSON
```

Do not lead with QEMU, judge packets, dashboard, or private runtime internals.
Those are operator details.

The slide should link or QR-code this package once the public URL exists.
